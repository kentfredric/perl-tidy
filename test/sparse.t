# a spaarse list
{
    $this->{'CURRENT'}{'gfx'}{'MatrixSkew'} = Text::PDF::API::Matrix->new(
        [ 1, tan( deg2rad($a) ), 0 ], [ tan( deg2rad($b) ), 1, 0 ],
        [ 0, 0,                  1 ]
    );
}

{
    {
        # sparsity .28
        $self->Target->Set("CharmMonster", 0, $self->Caster, $self->CharmTarget,
                           $self->CharmTarget2);
        {
            {
                _safe_symlink(
                    "$FindBin::Bin/$html_documentation",
                    "$destdir/$html_documentation"
                );
            }
        }
    }
}

# a small list of identifiers
(
  $scale,  $external, $thumbnail, $map,   $psimage, $align,
  $usemap, $flip,     $aalias,    $trans, $exscale, $alt,
  $_
);

# a small matrix with lots of white space 
my $xyzda = [
    [ $xm[ $j - 1 ], $ym[ $j - 1 ], $zm[ $j - 1 ] ],
    [ $xp[ $j - 1 ], $yp[ $j - 1 ], $zp[ $j - 1 ] ],
    [ $xp[$j],       $yp[$j],       $zp[$j] ],
    [ $xm[$j],       $ym[$j],       $zm[$j] ]
];

    # This is a current problem.
    # This will get formatted ok with 1 row perl line, but
    # the vertical aligner does do it correctly with 2 rows per line
    # try -l=60 to see it correctly formatted.
    $xyz_chip = [
        [ $umin,       $vmin,       $zmount ],
        [ $umin + $du, $vmin,       $zmount ],
        [ $umin + $du, $vmin + $dv, $zmount ],
        [ $umin,       $vmin + $dv, $zmount ],
    ];


    # These are some list canditates which are not unaligned
    # with the current empirical rules.

    # sparsity 0.29
    (
      $scale,  $external, $thumbnail, $map,   $psimage, $align,
      $usemap, $flip,     $aalias,    $trans, $exscale, $alt,
      $exstr ) = split ( '#', $img_params{$name} );

    # sparsity 0.55
    &embed_image(
      "${PREFIX}$img", $name, $external, $alt,
      $thumbnail,      $map,  $align,    $usemap,
      $exscale, $exstr );

    # sparsity ratio is 0.29
    (
      $scale,  $external, $thumbnail, $map,   $psimage, $align,
      $usemap, $flip,     $aalias,    $trans, $exscale, $alt,
      $_ );

    # sparsity ratio is 0.45
    &styled_text_chunk(
      '',             '',
      'font-variant', 'small-caps',
      '',             'process_smallcaps',
      @_ );

    # sparsity ratio is 0.45
    &styled_text_chunk(
      'U',               'CLASS="underline"',
      'text-decoration', 'underline',
      '',                '',
      @_ );

# This list is currently below the threshold for formatting
@LoL =
  ( [ "fred", "barney" ], [ "george", "jane", "elroy" ],
  [ "homer", "marge", "bart" ], );

# Another sparse list
    %html_specials = ( %html_specials
		, '``', ';SPMldquo;', "''", ';SPMrdquo;'
		) if ($HTML_VERSION >= 5 );

    # this gets messed up because it is too sparse
    %units = (
      "in",      "in", "pt",          "pt", "pc",           "pi",
      "mm",      "mm", "cm",          "cm", "\\hsize",      "%",
      "\\vsize", "%",  "\\textwidth", "%",  "\\textheight", "%"
    );

    # This comes out poorly because of the trailing hash in the list
    %styles_loaded = (
      'theorem', 1, 'enumerate',  1, 'a4paper', 1,
      'b5paper', 1, '10pt',       1, '11pt',    1,
      '12pt',    1, %styles_loaded
    );

