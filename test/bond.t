    { { {
    $sth->{'Database'}->{'xbase_tables'}
                  ->{ $parsed_sql->{'table'}[0] }->field_type($_);
    } } }
    # break this at a comma:
    $container->handler( Net::IRC::Event->new( 'dcc_open', $nick, $sock, 'get', 'get', $sock ) );

    # keep the anonymous hash block together:
    my $red_color = $widget->window->get_colormap->color_alloc( { red => 65000, green => 0, blue => 0 } );

    # don't break at .. if possible:
    foreach $ev( $self->queue ) {
        if ( $self->{_queue}->{$ev}->[0] <= $time ) {
            $self->{_queue}->{$ev}->[1]->( @{ $self->{_queue}->{$ev} }[ 2
              .. $#{ $self->{_queue}->{$ev} } ] );
            delete $self->{_queue}->{$ev};
        }
        else {
            $nexttimer = $self->{_queue}->{$ev}->[0]
              if ( $self->{_queue}->{$ev}->[0] < $nexttimer or not $nexttimer );
        }
    }

    # test break at ->'s   (bond strengths should allow leading arrows)
    $this->{Normals} = (double zeroes(3, (@{$this->{XYDims}}),
						@{$this->{OverDims}}))
			-> dummy(1,$this->{XYDims}[1]-1)
			-> dummy(1,$this->{XYDims}[1]-1)
			-> dummy(1,$this->{XYDims}[1]-1)
			-> dummy(1,$this->{XYDims}[1]-1)
			-> dummy(1,$this->{XYDims}[0]-1);

# test break at '} ('
{
      $type eq 'start_element' && do {
	  &{($start_element_handlers->{$event->data->name}||
		$start_element_handlers->{''} || sub {})}($event->data,$event);
	  last SWITCH;
      };
}

        my $magic_passphrase =
          join _ => split /\s+/ => join ~~~~~~~~~~chr 1 << 2**2 << 1 =>
          grep {y/a-zA-Z/n-za-mN-ZA-M/}
          reverse qw{
          cyrnfr    pneqf
          gur   nyy   unir
          gb   yvxr  ernyyl
          V'q
        };
